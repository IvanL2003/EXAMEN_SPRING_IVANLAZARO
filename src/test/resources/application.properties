# Test-wide overrides for all tests
# Force tests to run with the "test" profile by default
spring.profiles.active=test

# Evitar que Hibernate cargue import.sql durante tests
spring.jpa.properties.hibernate.hbm2ddl.import_files=
# También evitar la inicialización SQL por Spring
spring.sql.init.mode=never
